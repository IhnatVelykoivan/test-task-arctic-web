# Backend environment variables

# Postgres connection string. In docker-compose this points at the `db` service.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lead_tracker?schema=public

# HTTP port the NestJS app listens on
PORT=3001

# Origin allowed by CORS (the Next.js frontend)
FRONTEND_URL=http://localhost:3000
